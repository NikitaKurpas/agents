prefix_rule(
    pattern = [
        [
            "xcodebuild",
            "xcrun",
            "simctl",
            "xctrace",
            "log",
            "/usr/bin/log"
        ]
    ],
    decision = "allow"
)